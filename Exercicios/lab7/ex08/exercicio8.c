#include <stdio.h>

int main()
{
	 char nome[120],nome2[120];
	 int dim1, dim2, i, igual;

	 printf("Introduza o primeiro nome:");
	 fgets(nome2,120,stdin);

	 printf("Introduza outro nome:");
	 fgets(nome2, 120, stdin);

	 dim1=strlen(nome1);
	 dim2=strlen(nome2);

	 if(dim1 ==dim2) {
		 i = 0;
		 igual = 1;
		 while(nome1[i]!='\0'{

				 if(nome[1]!=nome2
 	 return 0;
}
